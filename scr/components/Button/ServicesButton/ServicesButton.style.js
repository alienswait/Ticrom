import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "#58bcf0",
        borderRadius: 10,
        padding: 10,
        margin: 10,
    },
    text: {
        color: "white",
        fontWeight: "bold",

    }
});